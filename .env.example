# Required for live API calls.
OPENAI_API_KEY=sk-your-key-here

# Optional. The default follows OpenAI's current model guidance but can be changed.
POLYMATH_MODEL=gpt-5.5

# Optional. Defaults to .skills inside the current workspace.
SKILLS_DIR=.skills
